// Cloudflare deploy for the static frontend: `npx craco build && npx wrangler deploy`
{
  "name": "alinazelinska",
  "compatibility_date": "2026-09-25",
  "workers_dev": true,
  "preview_urls": true,
  "routes": [
    { "pattern": "alinazelinska.com", "custom_domain": true },
    // www has its own proxied CNAME to the apex, so it's attached as a route
    { "pattern": "www.alinazelinska.com/*", "zone_name": "alinazelinska.com" }
  ],
  "assets": {
    "directory": "./build",
    // Client-side routes (/about, /services/…) all serve index.html
    "not_found_handling": "single-page-application"
  }
}
